# Puerto del servidor (Render asigna el suyo via PORT env var)
PORT=3001

# Sesión de Canva codificada en base64
CANVA_SESSION_BASE64=

# Fallback: credenciales por si expira la sesión
CANVA_EMAIL=costack.dev.bot@gmail.com
CANVA_PASSWORD=
